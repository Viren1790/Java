
package Day8;

public class Notes {
	
	/*Java errors 
	 * 3-Main errors 
	 * compile time error- It will happen while 
 * main-1-try{} - on code we can just add this. so if I thik in betwn this lines of code error
	 * if its shows error
	 * 
	 * INTERVIW-FINAL, FINAALY, FINILIZE
	 * interview-throw and throws keywords
	 * throw- its keyword is used to throw exception so we can through progrop(program stop karun exception show karu shaktoy)
	 * EX- if(b==0){  / throw new MyException("bivide by 0")
	 * how we can throw= through new MyException("Exception name")
	 * 
	 * throws- it will pass AirtictEcexception to solve use (try{} catch())
	 * throws- to declare one exception-ye exception througws kar sakta hai so you need to write code in try catch 
	 * 
	 * Day-9
	 * 
	 * when automation code fail how you can prove- we can prove By execution report(logs)
	 * -Java files handling
	 * Throgs keyword- will force the code to handle the method
	 * file cretating
	 * 
	 * 
	 * 
	 * 
	 * Day-14
	 * 
	 * java list
	 * dynamic arraylist 
	 * create java arrays list
	 * 
	 * How we can store link list in arrays
	 * sequence thorw if we want the list list
	 * 
	 * 
	 * Set-it does not following indexing as per list
	 * it stores only unique values
	 * there are three clesses-1hasset, 2-treeset, 3-linkedlist
	 * sequance matter karat naei kase pan add hotat values
	 * 
	 * 
	 * 
	 * Day15
	 * Map-interface- store elements in pare format eg(int, string)
	 * Hasmapp
	 * TreeMap
	 * LinkedHasMap
	 * 
	 * 
	 * 
	 * Day16
	 * Anyonimous-is a class without a name
	 * -whatever methied write in anynimous class it can not used again
	 * Enum-
	 * 
	 * 
	 * 
	 * Day17-IMP for selinium
	 * fileinput-
	 * Interview questions
	 * Filescanner
	 * WriteImagefile
	 * Fileoutput
	 * buffredReader
	 * bufferedwriter
	 * 
	 * 
	 * Day-19
	 * Revision-
	 * 
	 * 
	 * 
	 * 
	 * Day 20 
	 * Revision
	 * 
	 * constructor-same name as class name(Use to initilize class attributrs, On object we can add sparameter values)
	 * 
	 * this keyword- if class name and method variable name same-then we use this keyword so its call class varables.(this.vatablename)
	 * 
	 * modifiers- 
	 * access modifiers- it control the acces the methods
	 * public- on whole project we can call that object anywhere( to declare obj)
	 * default- we dont need to declare-the code is only access on same package anywhere calss but not outside the class
	 * private-access only in same class, it cannot be refer to class.
	 * protected- 
	 * 
	 * Non access modifier
	 * Final- we cannot change value if we declare final class, methos, varables.
	 * Static- if we use it on method, then we do not need to create that class objects. 
	 *just we can declare in main class like( methodname(); within the same class or same package but if its outside the package then we need to create on diffrent package)
	 *  Abstract- the method dont have any values in their method that we called abstract method
	 *  
	 *  
	 *    OOPS-Encapsulation
	 *    useing set , get methods and creation their objectes we can use private details.
	 *    
	 *    
	 *    Inheritance
	 *    
	 *- use ectends to access another class
	 * interface if we want to use=use implemented
	 * 
	 * 
	 * Polyimorphism
	 * -Mehods parameter with diffrent name its allowed
	 * -in one method their are 3 parameter then on obj we can call or add 3 values
	 * 
	 * 
	 * Interface is 100 abstract class
	 * 
	 * 
	 * 
	 * 
	 * 
	 * 
	 * 
	 * 
	 */

}
